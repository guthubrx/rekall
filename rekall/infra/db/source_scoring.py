# rekall/infra/db/source_scoring.py
"""
Source scoring and ranking calculations.

NOTE: Ce module est un placeholder pour la future extraction.
Actuellement, toute la logique est dans rekall.db.Database.

Future extraction - méthodes à extraire:
    - calculate_source_score(source_id) -> float
    - calculate_citation_quality(source_id) -> float
    - calculate_source_score_v2(source_id) -> float
    - update_citation_quality(source_id) -> bool
    - update_source_usage(source_id, entry_id) -> bool
    - recalculate_source_score(source_id) -> float
    - recalculate_all_scores() -> int
"""

from __future__ import annotations

# Placeholder - la logique reste dans rekall.db.Database
__all__: list[str] = []
