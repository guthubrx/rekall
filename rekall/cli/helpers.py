# rekall/cli/helpers.py
"""
CLI helper functions and utilities.

NOTE: Ce module est un placeholder pour la future extraction.
Actuellement, la logique est dans rekall.cli (fichier original).

Future extraction - fonctions à extraire:
    - show_banner() -> None
    - _check_and_fix_permissions(path: Path) -> bool
    - _display_validation_error(error: ValidationError) -> None
    - get_db() -> Database
    - get_research_topics() -> dict[str, str]

Constants à extraire:
    - BANNER: str
    - console: rich.console.Console
"""

from __future__ import annotations

# Placeholder - la logique reste dans rekall.cli (fichier original)
__all__: list[str] = []
