# rekall/cli/inbox.py
"""
Inbox sub-app and commands (Bronze layer).

NOTE: Ce module est un placeholder pour la future extraction.
Actuellement, la logique est dans rekall.cli (fichier original).

Future extraction:
    inbox_app = typer.Typer(...)               # Line 3367

    @inbox_app.command()
    - inbox_import() -> None                   # Line 3373
    - inbox_add(url, title) -> None            # Line 3522
    - inbox_stats() -> None                    # Line 3611
    - inbox_list(limit, status) -> None        # Line 3666
    - inbox_clear(status) -> None              # Line 3709
    - inbox_browse() -> None                   # Line 3779
"""

from __future__ import annotations

# Placeholder - la logique reste dans rekall.cli (fichier original)
__all__: list[str] = []
